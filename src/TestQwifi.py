import unittest
import qwifi

class TestQwifi(unittest.TestCase):
	def setup(self):
		#connect to test db
		#import test data
		#setup cursor
	def test_cull:
		#run the cull command (we should have expected data before and after)
		#query radcheck and make sure the data matches what we expect
	def test_dissasociate:
		#Not sure how to do this one. We need a way to automate hostapd session
	def test_updateRadcheck:
		#run updateRadcheck (we should have expected data before and after)
		##query radcheck and make sure the data matches what we expect

if __name__ == '__main__':
    unittest.main()